import { QueryResolvers } from "../../types.js";

// export const articles: QueryResolvers['articles'] = async (_, __, { dataSources }) => {
//   return await dataSources.db.article.findMany({
//     include: {
//       author: true,
//       comments: true,
//       likes: true,
//     }
//   });
// };
